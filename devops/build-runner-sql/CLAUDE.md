# 目的
OCI DevOps上のビルドランナーから、sqlコマンドを実行してOCIのAutonomous Databaseにあるデータ操作（ETL処理）が可能かどうかを検証する。
# アーキテクチャ
GitHub上にあるレポジトリに、ETL処理のためのSQLファイルが配置されている。レポジトリのメインブランチにsqlファイルの変更がマージされた時、OCI DevOpsのCIパイプラインを起動する。
# ルール
操作の手順は目的とともにdocsディレクトリにドキュメントを配置する。
